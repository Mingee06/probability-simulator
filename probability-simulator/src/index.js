import React from "react";
import ReactDOM from "react-dom";
import ProbabilitySimulator from "./App";

ReactDOM.render(
  <React.StrictMode>
    <ProbabilitySimulator />
  </React.StrictMode>,
  document.getElementById("root")
);